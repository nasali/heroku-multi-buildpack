#!/usr/bin/env ruby

$:.unshift File.expand_path("../../lib", __FILE__)
require "multipack"

puts "checking for #{ARGV[1]} in #{ARGV[0]}"
WORK_DIR = ARGV[0]
TRIGGER_FILE = ARGV[1]

if File.exists? WORK_DIR
  puts "FOUND!"
  exit 0
else
  puts "no"
  exit 1
end
