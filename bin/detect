#!/usr/bin/env ruby

$:.unshift File.expand_path("../../lib", __FILE__)
require "multipack"
puts "checking for #{ARGV[0]}"
if File.exists? ARGV[0]
  puts ARGV.first
  exit 0
else
  puts "no"
  exit 1
end
